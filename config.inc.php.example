<?php

// Location settings
$startingLat = "LATITUDE_HERE";                         // Starting latitude
$startingLng = "LONGITUDE_HERE";                        // Starting longitude

// MySQL database settings
$dbHost = "DATABASE_HOST";                              // MySQL host
$dbName = "DATABASE_NAME";                              // MySQL database name
$dbUser = "DATABASE_USERNAME";                          // MySQL username
$dbPass = "DATABASE_PASSWORD";                          // MySQL password

// Webserver settings
$title = "Pokémon Go Map";                              // Title to display in title bar
$locale = "en";                                         // Display language
                                                        // NOTE: For aesthetics only - I didn't implement code to change locale
                                                        //       outside of the lang specifier in the HTML. If you'd like to do so,
                                                        //       please refer to the i8ln(word) function in utils.py from
                                                        //       PokemonGo-Map and the get_pokemon_data($pokemon_id) function in
                                                        //       utils.php of this implementation.

// Misc
$gmapsKey = "API_KEY_HERE";                             // Google Maps API Key
$purgeData = 0;                                         // Clear pokemon from database this many hours after they disappear (0 to disable)

?>